## TO DO LIST

 - Consider migrating from the legacy Python 2 / dhtreader.so stack to a maintained
   DHT library (e.g. Adafruit CircuitPython or lgpio-based driver) for current Pi OS releases.

 - Externalize getData.php database credentials into a config file outside the web root.
